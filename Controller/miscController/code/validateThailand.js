/**
 * 
 * @param {{
 * province: String,
 * district: String,
 * subdistrict: String,
 * postcode: String,
 * }} data 
 */
const validateThailand = (
    data = {
        province,
        district,
        subdistrict,
        postcode,
    }
) => {
};